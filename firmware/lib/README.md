# Firmware libraries

This directory is reserved for firmware support libraries and modular components.

At the moment, no library modules are defined here.

Future library modules may include:

- board abstraction and pin contracts
- sensor drivers
- UI/status handling
- radio transport wrappers
- configuration and calibration storage
